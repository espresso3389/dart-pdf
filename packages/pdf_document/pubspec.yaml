name: pdf_document
description: >-
  Document-level PDF semantics: page tree, inherited attributes, metadata.
  Pure Dart, builds on pdf_cos.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  pdf_cos: ^0.1.0

dev_dependencies:
  pdf_test_fixtures: ^0.1.0
  lints: ^5.0.0
  test: ^1.25.0
