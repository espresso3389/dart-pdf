name: pdf_test_fixtures
description: >-
  Programmatically-built PDF files shared by the workspace's test suites.
  Builders compute offsets, so fixtures are always structurally correct.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  crypto: ^3.0.0
  pdf_cos: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
